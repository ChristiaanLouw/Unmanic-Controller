# Unmanic-Controller
A small Dockerized controller for Plex and Unmanic that pauses Unmanic workers during Plex playback, then automatically resumes them after media stops or pauses. Includes a simple web UI, Plex webhook listener, configurable Unmanic connection settings, logs, and Unraid-friendly Docker Compose examples.
